\subsection{MULT}

\Rinstr{000000}{ra}{rb}{rc}{rd}{001000}

\paragraph{Format} MULT ra, rb, rc, rd

\begin{lstlisting}[language=C]
    int64_t result =  (int64_t)GPR[rc] * (int64_t)GPR[rd];
    GPR[ra] = high(result);
    GPR[rb] = low(result);
\end{lstlisting}
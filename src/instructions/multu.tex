\subsection{MULTU}

\Rinstr{000000}{ra}{rb}{rc}{rd}{001001}

\paragraph{Format} MULTU ra, rb, rc, rd

\begin{lstlisting}[language=C]
    uint64_t result =  (uint64_t)GPR[rb] * (uint64_t)GPR[rc];
    GPR[ra] = high(result);
    GPR[rb] = low(result);
\end{lstlisting}
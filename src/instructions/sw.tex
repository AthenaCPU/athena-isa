\subsection{SW}

\LSinstr{001000}{ra}{rb}{10}{offset}

\paragraph{Format} SW offset(ra), rb

\paragraph{Purpose} Store the 32bit value from the low bits of register `rb` into memory

\begin{lstlisting}[language=C]
    *(int32_t *)(offset + GPR[ra]) = rb;
\end{lstlisting}
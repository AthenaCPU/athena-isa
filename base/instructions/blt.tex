\subsection{BLT}

\Iinstr{010000}{ra}{rb}{offset}

\paragraph{Format} BLT ra, rb, offset

\begin{lstlisting}[language=c]
    if ((int32_t)GPR[ra] < (int32_t)GPR[rb])
    {
        PC += (int32_t)offset * 4;
    }
\end{lstlisting}

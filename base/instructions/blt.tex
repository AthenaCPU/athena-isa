\subsection{BLT}

\Iinstr{010000}{ra}{rb}{offset}

\paragraph{Format} BLT ra, rb, offset

\begin{lstlisting}[language=c]
    if (GPR[ra] < GPR[rb])
    {
        PC += SEXT(offset) * 4;
    }
\end{lstlisting}

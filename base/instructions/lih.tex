\subsection{LIH}

\Iinstr{001011}{ra}{00000}{imm}

\paragraph{Format} LIH ra, imm

\paragraph{Purpose} Load a constant into the upper half of a word

\begin{lstlisting}[language=C]
    GPR[ra] = (GPR[ra] & ~(0xFFFF << 16)) | (imm << 16);
\end{lstlisting}
\subsection{SB}

\LSinstr{001000}{ra}{rb}{00}{offset}

\paragraph{Format} SB offset(ra), rb

\paragraph{Purpose} Store the 8bit value from the low bits of register `rb` into memory

\begin{lstlisting}[language=C]
    *(int8_t *)(offset + GPR[ra]) = rb;
\end{lstlisting}